# Go
/iugum

# Observe UI
web/observe/node_modules/
*.exe
*.dll
*.so
*.o
*.a
*.swp
*.swo
*.DS_Store
# Anchored to the repo root. An unanchored `bin/` also swallowed the
# `silverbullet/bin/` Rust crate directories in the vendored subtree.
/bin/
go-build/
*.pb.go
*.pb.gw.go

# Local prepare-pr artifacts
.iugum/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Go test binaries
*.test

# Agent worktrees
.claude/worktrees/

# Agent scratch harnesses, never source
scratchpad/

# The embedded SilverBullet server binary. Built by scripts/build-wiki-blob.sh.
# wikiblob/PLACEHOLDER is TRACKED so //go:embed always matches - see main.go.
/wikiblob/silverbullet
